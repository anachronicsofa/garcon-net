<%= simple_form_for @user do |f| %>
  <%= f.input :name, input_html: { maxlength: 30 } %>
  <%= f.input :password, input_html: { maxlength: 10 } %>
  <%= f.input :email %>
  <%=  f.check_box :admin, {}, true, false %>
  <%= f.button :submit %>
<% end %>

